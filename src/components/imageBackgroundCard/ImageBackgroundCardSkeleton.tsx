import { Skeleton } from '@mui/material';

const ImageBackgroundCardSkeleton = () => (
  <Skeleton variant="rounded" width="100%" height="10rem" sx={{ borderRadius: '1rem'}} />
);

export default ImageBackgroundCardSkeleton;
